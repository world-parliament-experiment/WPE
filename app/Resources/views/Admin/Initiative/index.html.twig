{% extends "base.html.twig" %}

{% block body %}

    <!-- ##### Breadcrumb Area Start ##### -->
    <section class="breadcrumb-area bg-img bg-overlay"
             style="background-image: url('https://via.placeholder.com/1920x850');">
        <div class="container h-100">
            <div class="row h-100 align-items-center">
                <div class="col-12">
                    <div class="breadcrumb-content">
                        <h2>Initiatives</h2>
                    </div>
                </div>
            </div>
        </div>
    </section>
    <!-- ##### Breadcrumb Area End ##### -->

    <!-- ##### Login Area Start ##### -->
    <div class="mag-login-area py-5">
        <div class="container">
            <div class="row justify-content-center">
                <div class="col-12">
                    <div class="login-content bg-white p-30 box-shadow">
                        <!-- Section Title -->
                        <div class="section-heading">
                            <h5>Initiatives Overview!</h5>
                        </div>

                        {% trans_default_domain 'messages' %}

                            <h5>Initiatives:</h5>

                            <table id="initiatives" class="table table-striped table-bordered" style="width:100%">
                                <thead>
                                <tr>
                                    <th>id</th>
                                    <th>name</th>
                                    <th>description</th>
                                    <th>type</th>
                                    <th>state</th>
                                </tr>
                                </thead>

                                <tbody>
{#                                {% for initiative in initiatives %}#}
{#                                    <tr>#}

{#                                        <td>#}
{#                                            <a href="{{ path('admin_initiative_edit', { 'id': initiative.id }) }}">{{ initiative.id }}</a>#}
{#                                        </td>#}
{#                                        <td>{{ initiative.title }}</td>#}
{#                                        {% autoescape false %}#}
{#                                            <td>{{ initiative.description|raw|truncate(50, true) }}</td>#}
{#                                        {% endautoescape %}#}
{#                                        <td>{{ initiative.stateName }}</td>#}
{#                                        <td>{{ initiative.typeName }}</td>#}
{#                                    </tr>#}

{#                                {% endfor %}#}
                                </tbody>

                                <tfoot>
                                <tr>
                                    <th>id</th>
                                    <th>name</th>
                                    <th>description</th>
                                    <th>type</th>
                                    <th>state</th>
                                </tr>
                                </tfoot>

                            </table>
                        </div>

                    </div>
                </div>
            </div>
        </div>
    </div>
    <!-- ##### Login Area End ##### -->

{% endblock body %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" type="text/css" href="{{ asset('assets/plugins/DataTables/datatables.min.css') }}"/>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script type="text/javascript" src="{{ asset('assets/plugins/DataTables/datatables.min.js') }}"></script>
    <script>
        $(document).ready(function () {
            const table = $('#initiatives').DataTable({
                responsive: false,
                autoWidth: true,
                "processing": true,
                "serverSide": true,
                "searching": true,
                "ordering": true,
                "ajax": {
                    "url": Routing.generate('admin_initiative_search'),
                    "dataSrc": "items",
                    "type": "POST"

                },
                "columns": [
                    {
                        "data": "id",
                        "fnCreatedCell": function (nTd, sData, oData, iRow, iCol) {
                            $(nTd).html("<a href='" + Routing.generate('admin_initiative_edit', {id: oData.id}) + "'>" + oData.id + "</a>");
                        }
                    },
                    {"data": "shorttitle"},
                    {"data": "shortdescription"},
                    {"data": "typeName"},
                    {"data": "stateName"}
                ],
                "order": [[1, "asc"]],
                deferRender: true,
            });
        });
    </script>

{% endblock %}
